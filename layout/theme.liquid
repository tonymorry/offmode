
<!DOCTYPE html>
<html lang="{{ request.locale.iso_code }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="canonical" href="{{ canonical_url }}">
    <title>{{ page_title }} - {{ shop.name }}</title>

    {{ content_for_header }}

    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Crimson+Pro:ital,wght@0,600;1,600&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --bg-cream: #f9f7f2;
            --text-dark: #1a1a1a;
            --accent-muted: #4a5d4e;
        }
        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-cream);
            color: var(--text-dark);
            -webkit-font-smoothing: antialiased;
        }
        .serif { font-family: 'Crimson Pro', serif; }
        .vintage-shadow { box-shadow: 0 10px 30px -10px rgba(0,0,0,0.1); }
        [id^="shopify-section-"] { position: relative; z-index: 1; }
    </style>
</head>
<body class="bg-[#f9f7f2]">
    {% section 'header' %}
    
    <main id="MainContent" role="main">
        {{ content_for_layout }}
    </main>

    {% section 'footer' %}
</body>
</html>
